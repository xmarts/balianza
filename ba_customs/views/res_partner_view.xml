<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_res_partner_driver_ba" model="ir.ui.view" >
        <field name="name">res.partner.view</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <field name="ref" position="after">
                <field name="is_driver" />
            </field>
        </field>
    </record>

    <record id="res_partner_action_driver_ba" model="ir.actions.act_window">
        <field name="name">Conductores</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="context">{'is_driver': True}</field>
        <field name="domain">[('is_driver', '=', True)]</field>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            Create a new customer in your address book
          </p><p>
            Odoo helps you easily track all activities related to a customer.
          </p>
        </field>
    </record>


</odoo>